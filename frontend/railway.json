{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "healthcheckPath": "/",
    "startCommand": "npm run preview -- --host 0.0.0.0 --port $PORT"
  }
}
